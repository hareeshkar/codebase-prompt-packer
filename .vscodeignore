# .vscodeignore

# Source files (we only need the compiled output)
src/

# Keep the compiled output
!out/

# Ignore build and config files
tsconfig.json
.github/
.vscode/

# Ignore git files
.git*

# Ignore test files (if you add them later)
*.test.ts